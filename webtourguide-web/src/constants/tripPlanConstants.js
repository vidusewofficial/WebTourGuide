/**
 * Constants used across the Trip Planning module.
 * Import these instead of hardcoding magic numbers in components.
 */

/** Maximum number of characters allowed in a trip plan title. */
export const MAX_TRIP_TITLE_LENGTH = 150;

/** Maximum number of days that can be added to a single trip plan. */
export const MAX_DAYS_PER_TRIP = 30;
