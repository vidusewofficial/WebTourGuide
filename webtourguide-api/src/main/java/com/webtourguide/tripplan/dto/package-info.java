/**
 * Data Transfer Objects for the Trip Planning module.
 * These classes carry data between the REST controller and the client;
 * they are never persisted directly to the database.
 */
package com.webtourguide.tripplan.dto;
